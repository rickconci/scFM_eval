# scGPT extractor dependencies
# 
# IMPORTANT: scGPT requires torch >= 1.13.0
# For CUDA 12.8, use PyTorch with cu128 support
# Install in a fresh environment:
#
#   python -m venv env
#   source env/bin/activate
#   pip install torch torchvision torchtext
#   pip install -r requirements.txt
#   pip install -e /lotterlab/users/riccardo/ML_BIO/Bio_FMs/RNA/scGPT
#
# Note: PyTorch is installed separately above to ensure CUDA 12.8 compatibility
# The versions below are installed via the cu128 index

# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scanpy>=1.9.0
anndata>=0.9.0
tqdm>=4.66.0
safetensors>=0.3.0

# scGPT dependencies
scvi-tools>=0.16.0,<1.0
scikit-misc>=0.1.4
ipython>=8.0.0  # Required by scGPT utils
torchtext>=0.18.0  # Required by scGPT tokenizer

# Survival analysis
scikit-survival>=0.23.0

# Install scGPT from local repo:
# pip install -e /lotterlab/users/riccardo/ML_BIO/Bio_FMs/RNA/scGPT
