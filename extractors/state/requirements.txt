# STATE (arc-state) extractor dependencies
#
# IMPORTANT: STATE requires scipy>=1.15, which conflicts with lamin-dataloader
# Install in a fresh environment:
#
#   python -m venv env
#   source env/bin/activate
#   pip install -r requirements.txt
#   pip install -e /lotterlab/users/riccardo/ML_BIO/Bio_FMs/RNA/state

# PyTorch
torch>=2.0.0

# Scipy - STATE requires newer version
scipy>=1.15.0

# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
scanpy>=1.9.0
anndata>=0.9.0
tqdm>=4.66.0

# STATE dependencies
transformers>=4.30.0
peft>=0.11.0
hydra-core>=1.3.0
wandb>=0.16.0
scikit-learn>=1.0.0

# Survival analysis
scikit-survival>=0.23.0

# Install STATE from local repo:
# pip install -e /lotterlab/users/riccardo/ML_BIO/Bio_FMs/RNA/state
